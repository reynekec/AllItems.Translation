<Application x:Class="AllItems.Translation.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:AllItems.Translation.App.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <converters:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter" />
            <converters:NullOrEmptyToBoolConverter x:Key="NullOrEmptyToBoolConverter" />
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
            <converters:EnumMatchToVisibilityConverter x:Key="EnumMatchToVisibilityConverter" />
            <converters:BoolToButtonAppearanceConverter x:Key="BoolToButtonAppearanceConverter" />
            <converters:MultipleChoiceVisualStateToButtonAppearanceConverter x:Key="MultipleChoiceVisualStateToButtonAppearanceConverter" />
            <converters:SlotCorrectnessToTextConverter x:Key="SlotCorrectnessToTextConverter" />

            <Style x:Key="WordChipButtonStyle" TargetType="ui:Button">
                <Setter Property="Margin" Value="0,0,4,4" />
                <Setter Property="Padding" Value="8,3" />
                <Setter Property="FontSize" Value="16" />
            </Style>

            <Style x:Key="AccentSettingsButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#2F6B5F" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="#2F6B5F" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="10,4" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#3B8678" />
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#3B8678" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#255349" />
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#255349" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
